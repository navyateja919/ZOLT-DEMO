<%- include("partials/header-products.ejs") %>
    <!-- Your page content goes here -->
    <div class="product-page">
        <div class="product-page-header">
            <div class="tog-box">
                <div id="tog-bg"></div>
                <button onclick="leftClick()" class="btn-mg">8mg</button>
                <button onclick="rightClick()" class="btn-mg">15mg</button>
            </div>
            
        </div>
        <div id="products-list">
            <div id="products-list-8mg">        
                <% products.forEach(function product (value) { %>
                        <div class="item">
                            <div class="product-img">
                                <h4><%= value.product_name %></h4>
                                <img src="images/products/<%= value.product_image %>" alt="">
                                <div class="product-details">
                                    <span class="spn">8mg</span>
                                    <h5>Price: <span>$<%= value.product_price %></span></h5>
                                    
                                    <form method="post" action="/add-cart">
                                        <input type="hidden" name="product_id" value="<%= value.id %>">
                                        <input type="hidden" name="product_name" value="<%= value.product_name %>">
                                        <input type="hidden" name="product_price" value="<%= value.product_price %>">
                                        <input type="submit" class="btn btn-warning" value="Add To Cart">
                                    </form>    
                                </div>     
                            </div>               
                        </div>
                <% }) %>
            </div>
            <div id="products-list-15mg">        
                <% products-15mg.forEach(function product (value) { %>
                        <div class="item">
                            <div class="product-img">
                                <h4><%= value.product_name %></h4>
                                <img src="images/products/<%= value.product_image %>" alt="">
                                <div class="product-details">
                                    <span class="spn">15mg</span>
                                    <h5>Price: <span>$<%= value.product_price %></span></h5>
                                    
                                    <form method="post" action="/add-cart">
                                        <input type="hidden" name="product_id" value="<%= value.id %>">
                                        <input type="hidden" name="product_name" value="<%= value.product_name %>">
                                        <input type="hidden" name="product_price" value="<%= value.product_price %>">
                                        <input type="submit" class="btn btn-warning" value="Add To Cart">
                                    </form>    
                                </div>     
                            </div>               
                        </div>
                <% }) %>
            </div>
        </div>
        </div>
        </div>
        
        

<%- include("partials/footer-products.ejs") %>    
